diff --git a/01-postgres.yaml b/01-postgres.yaml
index 1234567..89abcde 100644
--- a/01-postgres.yaml
+++ b/01-postgres.yaml
@@ -120,6 +120,12 @@ spec:
                 psql -v ON_ERROR_STOP=1 --username postgres --dbname postgres <<-'EOSQL'
                     CREATE USER replicator WITH REPLICATION ENCRYPTED PASSWORD '${REPLICATION_PASSWORD}';
                     CREATE USER pdns WITH ENCRYPTED PASSWORD '${PDNS_PASSWORD}';
+                    -- Set the postgres superuser password so remote md5 auth works
+                    ALTER USER postgres WITH ENCRYPTED PASSWORD '${POSTGRES_PASSWORD}';
+
+                    -- Optional: ensure extensions and roles exist before schema init
+                    CREATE EXTENSION IF NOT EXISTS pg_stat_statements;
+
                     CREATE DATABASE powerdns OWNER pdns;
                     GRANT ALL PRIVILEGES ON DATABASE powerdns TO pdns;
                 EOSQL
@@ -230,7 +236,20 @@ spec:
           env:
             - name: DATA_SOURCE_NAME
-              value: "postgresql://postgres:$(POSTGRES_PASSWORD)@localhost:5432/postgres?sslmode=disable"
+              valueFrom:
+                secretKeyRef:
+                  name: postgres-secret
+                  key: postgres-password
+          command: ["/bin/sh", "-c"]
+          args:
+            - |
+              echo "Starting postgres-exporter..."
+              export DATA_SOURCE_NAME="postgresql://postgres:${POSTGRES_PASSWORD}@localhost:5432/postgres?sslmode=disable"
+              exec /bin/postgres_exporter
+
+---
+# Reduce the PDB minAvailable to allow single-instance operation
+apiVersion: policy/v1
+kind: PodDisruptionBudget
+metadata:
+  name: postgres-pdb
+spec:
+  minAvailable: 1
+  selector:
+    matchLabels:
+      app: postgres
diff --git a/02-postgres-pdns-init.yaml b/02-postgres-pdns-init.yaml
index 2345678..90defab 100644
--- a/02-postgres-pdns-init.yaml
+++ b/02-postgres-pdns-init.yaml
@@ -33,7 +33,7 @@ spec:
             echo "Creating PowerDNS schema..."
             PGPASSWORD="${PDNS_PASSWORD}" psql -h postgres-primary -U "${PDNS_USER}" -d "${PDNS_DATABASE}" -v ON_ERROR_STOP=1 <<-'EOF'
             -- PowerDNS schema creation
-            CREATE TABLE domains (
+            CREATE TABLE IF NOT EXISTS domains (
               id                    SERIAL PRIMARY KEY,
               name                  VARCHAR(255) NOT NULL,
               master                VARCHAR(128) DEFAULT NULL,
@@ -80,7 +80,7 @@ spec:
               change_date           INT DEFAULT NULL
             );
             CREATE INDEX records_domain_id_idx ON records(domain_id);
-            EOSQL
+            EOF

             echo "✅ PowerDNS schema initialization complete."
